# Feature Backlog

Ordered by priority. Top item is next to be developed.

- `browser-note-storage` — Persist browser notes as Markdown documents with
  YAML frontmatter through the storage abstraction.
